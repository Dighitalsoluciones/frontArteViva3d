import { CondicionAdmDirective } from './condicion-adm.directive';

describe('CondicionAdmDirective', () => {
  it('should create an instance', () => {
    const directive = new CondicionAdmDirective();
    expect(directive).toBeTruthy();
  });
});
